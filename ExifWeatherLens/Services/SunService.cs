using ExifWeatherLens.Interfaces;
using ExifWeatherLens.Models;

namespace ExifWeatherLens.Services;

public class SunService : ISunService
{
    public SunInfo GetSunInfo(DateTime dateTime, double latitude, double longitude)
    {
        
        
        return new SunInfo();
    }
}